[🇬🇧 Read in English](ENGreadme.md)
# SQLite Удаление дубликатов с Node.JS врапом

[![Node.js](https://img.shields.io/badge/Node.js-18+-green.svg)](https://nodejs.org/)
[![SQLite](https://img.shields.io/badge/SQLite-3.25+-blue.svg)](https://www.sqlite.org/)

---

## Описание

Скрипт демонстрирует три различных подхода к удалению дубликатов записей в базе данных SQLite. Каждый метод имеет свои преимущества и подходит для разных сценариев использования.
Проект включает SQL-скрипты и Node.js обёртку для удобного запуска и интеграции в CI/CD.

---

## Методы удаления дубликатов

| Метод | Технология |
|-------|------------|
| **Метод 1** | `DELETE` + подзапрос с `MIN(id)` |
| **Метод 2** | Оконные функции `ROW_NUMBER()` |
| **Метод 3** | CTE + `ROW_NUMBER()` |

---

## Структура проекта

```
main/
├── run-script.js               # Скрипт для запуска
├── remove_duplicates.sql       # Основной SQL скрипт
├── database.js
├── index.js                    # Точка входа
├── package.json
├── package-lock.json
├── .gitignore
└── cleanup.db                  # SQLite база (создаётся при запуске)
```

---

## Установка и запуск

### 1. Установка зависимостей

```bash
cd sql_cleanup
npm install
```

### 2. Запуск

```bash
# Основной скрипт
npm start

# Или только удаление дубликатов
npm run duplicates
```

### 3. Запуск без npm (Windows PowerShell)

```powershell
node index.js
```

---

## Заметки

- Все методы сохраняют запись с **минимальным ID**
- Скрипт включает тестовые данные для демонстрации
- После выполнения база удаляется (для чистого теста)

---
